#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <pulsecore/log.h>

void pa_log_levelv_meta(
        pa_log_level_t level,
        const char*file,
        int line,
        const char *func,
        const char *format,
        va_list ap)
{
}
